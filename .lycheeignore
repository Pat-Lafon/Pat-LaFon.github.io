# URLs to skip during link checking. One regex per line.

# Expired intermediate; rustls rejects the chain, browsers paper over it.
pl\.cs\.uchicago\.edu

# Host down.
guidetojapanese\.org

# HTTP-only and rejects non-browser clients; /deadlines/ links many events there.
wikicfp\.com
